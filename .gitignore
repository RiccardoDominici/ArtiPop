node_modules/
.wrangler/
.dev.vars
.DS_Store

# Il tool di tuning (tuning/) e' versionato: e' uno strumento del progetto.
# Restano fuori solo eventuali copie del JSON dei range scaricate a mano.
tuning/*.json

# Config gemella per pilotare la produzione da locale (bindings remoti).
backend/wrangler.remote.jsonc
